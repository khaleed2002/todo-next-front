/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: [
      'res.cloudinary.com',
      'https://a-khaled-abdelrahman2023-gmail-com-api.appasapserver.app',
    ],
  },
}

export default nextConfig
